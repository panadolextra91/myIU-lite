// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: assignments.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createAssignment = `-- name: CreateAssignment :one
INSERT INTO assignments (
    course_id, title, description, deadline, accept_late, late_threshold_days, created_by
) VALUES (
    $1, $2, $3, $4, $5, $6, $7
) RETURNING id, course_id, title, description, deadline, accept_late, late_threshold_days, created_by, created_at, updated_at
`

type CreateAssignmentParams struct {
	CourseID          int64
	Title             string
	Description       pgtype.Text
	Deadline          pgtype.Timestamptz
	AcceptLate        bool
	LateThresholdDays pgtype.Int4
	CreatedBy         int64
}

func (q *Queries) CreateAssignment(ctx context.Context, arg CreateAssignmentParams) (Assignment, error) {
	row := q.db.QueryRow(ctx, createAssignment,
		arg.CourseID,
		arg.Title,
		arg.Description,
		arg.Deadline,
		arg.AcceptLate,
		arg.LateThresholdDays,
		arg.CreatedBy,
	)
	var i Assignment
	err := row.Scan(
		&i.ID,
		&i.CourseID,
		&i.Title,
		&i.Description,
		&i.Deadline,
		&i.AcceptLate,
		&i.LateThresholdDays,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getAssignmentByID = `-- name: GetAssignmentByID :one
SELECT a.id, a.course_id, a.title, a.description, a.deadline, a.accept_late, a.late_threshold_days, a.created_by, a.created_at, a.updated_at
FROM assignments a
JOIN courses c ON a.course_id = c.id
WHERE a.id = $1 AND c.deleted_at IS NULL
`

func (q *Queries) GetAssignmentByID(ctx context.Context, id int64) (Assignment, error) {
	row := q.db.QueryRow(ctx, getAssignmentByID, id)
	var i Assignment
	err := row.Scan(
		&i.ID,
		&i.CourseID,
		&i.Title,
		&i.Description,
		&i.Deadline,
		&i.AcceptLate,
		&i.LateThresholdDays,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listCourseAssignments = `-- name: ListCourseAssignments :many
SELECT a.id, a.course_id, a.title, a.description, a.deadline, a.accept_late, a.late_threshold_days, a.created_by, a.created_at, a.updated_at
FROM assignments a
JOIN courses c ON a.course_id = c.id
WHERE a.course_id = $1 AND c.deleted_at IS NULL
ORDER BY a.created_at DESC
`

func (q *Queries) ListCourseAssignments(ctx context.Context, courseID int64) ([]Assignment, error) {
	rows, err := q.db.Query(ctx, listCourseAssignments, courseID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Assignment
	for rows.Next() {
		var i Assignment
		if err := rows.Scan(
			&i.ID,
			&i.CourseID,
			&i.Title,
			&i.Description,
			&i.Deadline,
			&i.AcceptLate,
			&i.LateThresholdDays,
			&i.CreatedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
